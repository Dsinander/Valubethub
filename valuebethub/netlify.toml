[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Environment variable API_FOOTBALL_KEY must be set in Netlify dashboard
